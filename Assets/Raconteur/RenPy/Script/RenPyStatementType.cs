namespace DPek.Raconteur.RenPy.Script
{
	[System.Serializable]
	public enum RenPyStatementType {
		BG, CALL, CHARACTER, COMMENT, ELIF, ELSE, HIDE, IF, IMAGE, INIT, JUMP,
		LABEL, LINE, MENU, MENU_CHOICE, PASS, PAUSE, PLAY, QUEUE, RETURN, SAY,
		SCENE, SHOW, STOP, VARIABLE, WHILE, WINDOW
	};
}
